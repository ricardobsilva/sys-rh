<%= form_for(@type_patio) do |f| %>
  <% if @type_patio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@type_patio.errors.count, "error") %> prohibited this type_patio from being saved:</h2>

      <ul>
      <% @type_patio.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label 'nome' %><br>
    <%= f.text_field :name , class: 'form-control' %>
  </div>
  <div class="field">
    <%= f.label 'iniciais' %><br>
    <%= f.text_field :initial, class: 'form-control' %>
  </div>
  <br>
  <div class="actions">
    <%= f.submit "salvar" , class: 'btn btn-success' %> |
    <%= link_to 'voltar', type_patios_path %>
  </div>
<% end %>
