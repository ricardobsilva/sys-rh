<p id="notice"><%= notice %></p>

<h1>Regiões Administrativas</h1>

<%= link_to 'novo registro', new_administrative_region_path , class: 'btn btn-sm btn-primary pull-right' %>
<table class=" table table-striped">
  <thead>
    <tr>
      <th>Nome</th>
      <th>Iniciais</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @administrative_regions.each do |administrative_region| %>
      <tr>
        <td><%= administrative_region.name %></td>
        <td><%= administrative_region.initials %></td>
        <td>
        <div class="btn-group pull-right">
          <%#= link_to 'Show', administrative_region %>
          <%= link_to 'editar', edit_administrative_region_path(administrative_region) , class: 'btn btn-xs btn-warning' %>
          <%= link_to 'deletar', administrative_region, method: :delete, data: { confirm: 'Quer mesmo excluir?' }, class: 'btn btn-xs btn-warning' %>
        </div>  
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

