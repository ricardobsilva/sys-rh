<%= form_for(@instituation) do |f| %>
  <% if @instituation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@instituation.errors.count, "error") %> prohibited this instituation from being saved:</h2>

      <ul>
      <% @instituation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
