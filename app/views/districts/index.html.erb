<p id="notice"><%= notice %></p>

<h1>Bairros</h1>
<%= link_to 'novo registro', new_district_path ,  class: 'btn btn-sm btn-primary pull-right' %>

<table class=" table table-striped">
  <thead>
    <tr>
      <th>Nome</th>
      <th>Região Administrativa</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @districts.each do |district| %>
      <tr>
        <td><%= district.name %></td>
        <td><%= district.administrative_region_id %></td>
        <td>
        <div class="btn-group pull-right">
          <%#= link_to 'Show', district %>
          <%= link_to 'editar', edit_district_path(district), class: 'btn btn-xs btn-warning' %>
          <%= link_to 'deletar', district, method: :delete, data: { confirm: 'quer mesmo deletar?' } , class: 'btn btn-xs btn-warning' %>
        </div>  
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

